---
layout: default
items:
  - id: 1
    label: Spec out a high level pan of attack
    tags:
      - urgent
      - blocked
  - id: 2
    label: Add time estimates to the various parts of the project
    tags:
      - blocked
      - notifies
  - id: 3
    label: Choose a technical infrastructure for the project
    tags:
      - urgent
      - notifies
  - id: 4
    label: Spin up an example app
    tags:
      - notifies
---

<div
  v-component="to-do-list"
  class="border-2 rounded bg-white"
>
  <div class="flex items-center px-8 py-4 border-b-2">
    <p class="text-sm text-gray-700 font-semibold uppercase">List of To-Dos</p>
    <div class="w-8"></div>
    <%- include('/to-do/search') %>
    <div class="w-8"></div>
    <select v-sync="filter" >
      <option value="">All tags...</option>
      <option value="urgent">URGENT</option>
      <option value="blocked">BLOCKED</option>
      <option value="notifies">NOTIFIES</option>
    </select>
    <div class="w-2"></div>
    <select v-sync="sort">
      <option value="">Default order</option>
      <option value="alphaAsc">Alphabetical (A-Z)</option>
      <option value="alphaDesc">Alphabetical (Z-A)</option>
    </select>
  </div>
  <div
    class="p-8"
    v-sort="orderBy"
    v-filter="filterBy"
    v-ref="list"
  >
    <% items.forEach(item => { %>
      <%- include('/to-do/item', { toDo: item }) %>
    <% }); %>
  </div>
  <div class="px-8 py-4 border-t-2">
    <button
      v-if="!creating"
      class="button-indigo"
      v-event:click="startCreating"
    >New To-Do</button>
    <div v-if="creating">
      <%- include('/to-do/new-item') %>
    </div>
  </div>
</div>
